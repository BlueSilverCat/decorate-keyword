/**
 * 日本語のコメント Japanese Comment
 * @param {number} x
 */
function hoge(x: number): number {
  return x * x;
}

/**
 * 日本語のコメント Japanese Comment
 * @param {number} x
 */
function hage(x: number): number {
  return x + x;
}
